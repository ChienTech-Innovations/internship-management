# NEXT_PUBLIC_API_BASE_URL is baked into the JS bundle at build time.
# When running via docker compose, this is set via build.args — no .env needed.
# Only set here when running `npm run dev` locally outside Docker.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
